// Stub — rendered but hidden until a camera is selected (Milestone 6)
export default function RightEditPanel() {
  return (
    <aside
      className="shrink-0 bg-slate-800 border-l border-slate-700"
      style={{ width: 0, overflow: 'hidden' }}
      aria-hidden="true"
    />
  )
}
