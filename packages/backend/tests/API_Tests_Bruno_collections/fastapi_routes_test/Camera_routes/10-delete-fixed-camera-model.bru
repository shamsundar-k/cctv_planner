meta {
  name: delete-fixed-camera-model
  type: http
  seq: 10
}

delete {
  url: http://localhost:8000/api/v1/camera-models/{{fixed_camera_model_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 204
}
