meta {
  name: delete-project-success
  type: http
  seq: 10
}

delete {
  url: http://localhost:8000/api/v1/projects/{{project_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 204
}
