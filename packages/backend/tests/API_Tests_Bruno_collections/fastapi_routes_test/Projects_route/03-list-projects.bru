meta {
  name: list-projects
  type: http
  seq: 3
}

get {
  url: http://localhost:8000/api/v1/projects
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 200
  res.body: isArray
}
