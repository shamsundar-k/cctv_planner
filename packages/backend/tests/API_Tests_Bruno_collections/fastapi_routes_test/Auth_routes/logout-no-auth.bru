meta {
  name: logout-no-auth
  type: http
  seq: 7
}

post {
  url: http://localhost:8000/api/v1/auth/logout
  body: json
  auth: none
}

body:json {
  {
    "refresh_token": "any_token_here"
  }
}

assert {
  res.status: eq 403
  res.body.detail: contains "Not authenticated"
}
